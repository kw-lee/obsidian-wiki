services:
  backend:
    build:
      target: dev
    volumes:
      - ./backend:/app
      - vault_data:/data/vault
      - config_data:/data/config

  frontend:
    build:
      target: dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "24678:24678"
